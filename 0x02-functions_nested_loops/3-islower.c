#include "main.h"

/**
* _islower - It compares lower case letter
* 
* c - its is parameter passed on the function
*
* Return: Always 0
*/
int _islower(int c)
{
if (c >= 'a' && c <= 'z')
{
return (1);
}
else
{
return (0);
}
}
