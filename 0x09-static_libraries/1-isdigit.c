#include <stdio.h>

/**
 * _isdigit - print upper case letters.
 *
 * @c: parameter passed through the function isupper.
 * Return: Always 0.
 */
int _isdigit(int c)
{
	return (c >= '0' && c <= '9');

}
